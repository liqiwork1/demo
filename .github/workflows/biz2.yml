name: biz2

on:
  push:
    branches: [main]

jobs:
  job1:
    uses: ./.github/workflows/biz1.yml
  job2:
    uses: ./.github/workflows/env.yml
  job3:
    runs-on: ubuntu-latest
    needs: job2
    steps:
      - run: echo  "${{ needs.job2.outputs.firstword }}  ${{ needs.job2.outputs.secondword }} in the biz2."
