name: biz1

on:
  workflow_call:
    outputs:
        biz1:
          description: "The first output string"
          value: "env3"
  push:
    branches: [main]

jobs:
  job1:
    uses: ./.github/workflows/env.yml

  job2:
    runs-on: ubuntu-latest
    needs: job1
    steps:
      - run: echo  "${{ needs.job1.outputs.firstword }} ${{ needs.job1.outputs.secondword }} in the biz1."
